import styled from "styled-components";

const AboutWrapper = styled.div`
  .heading {
    margin: 20 px;
  }
  .desc {
    margin: 50px auto;
    line-height: 2.75;
  }
  .year {
    float: right;
  }
`;
export default AboutWrapper;
